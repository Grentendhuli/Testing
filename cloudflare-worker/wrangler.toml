name = "landlordbot-ai"
main = "landlordbot-ai.js"
compatibility_date = "2024-03-01"

# Cloudflare Workers AI is available on the free tier
[ai]
binding = "AI"

# Optional: KV namespace for distributed rate limiting
# Uncomment and configure after creating KV namespace in Cloudflare dashboard
# [[kv_namespaces]]
# binding = "RATE_LIMIT_KV"
# id = "your-kv-namespace-id"

# CORS headers are handled in the worker code
# The worker will respond to requests from your Vercel domain

[env.production]
# Production environment
# Add KV namespace binding here for production

[env.staging]
# Staging environment
# Add separate KV namespace binding here for staging
